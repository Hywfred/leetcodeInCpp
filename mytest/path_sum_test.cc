#include "daily.h"
#include "gtest/gtest.h"

using namespace std;

TEST(mytest, path_sum) {
    // vector<int> data{5, 4, 8, 11, INT_MAX, 13, 4, 7, 2, INT_MAX, INT_MAX, INT_MAX, 1};
    // auto root = GenerateBT(data);
    // auto ans = hasPathSun(root, 22);
    // EXPECT_TRUE(ans);
}